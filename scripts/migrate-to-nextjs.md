# Migration Notes

- Replaced Vite entry architecture with Next.js App Router (`app/`).
- Added reusable components in `components/`.
- Added educational IA pages: start-here, roadmap, lessons, projects, notes, about.
- Added basic interactive visualizations for vectors, dot product, gradient descent, and decision boundaries.
